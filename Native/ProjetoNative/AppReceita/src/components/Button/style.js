import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        marginTop: 20,
        backgroundColor: '#555',
        paddingVertical: 15,
        paddingHorizontal: 25,
        borderRadius: 5,
    },
});